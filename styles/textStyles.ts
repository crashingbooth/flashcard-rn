import { StyleSheet } from 'react-native';

export const textStyles = StyleSheet.create({
    subHeaderText: {
        fontSize: 16,
        fontWeight: 'bold'
    },
    headerText: {
        fontSize: 20,
        fontWeight: 'bold',
        margin: 5
    },
  });